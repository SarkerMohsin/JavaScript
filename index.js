// Create variables for first and last Name.
const firstName = "Mohsin";
const lastName = "Sarker";

// Calling variables as an alert during openning the page.
alert("Welcome: " + firstName + " " + lastName);
console.log("Welcom to the page");

// Writing text on the document.
document.write("Writing something here.");


// Creating variables with the key words of var, let and const
const personName = "John";
let age = 35;
var nameAndAge = personName + " is " + age + " years old.";

console.log(nameAndAge);

//Creating variables with arithmetic operators.

let x = 10;
let y = 15;
const result = x + y;
const remainder = x % y;

console.log(remainder);
console.log(result);